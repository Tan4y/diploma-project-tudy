package org.tues.tudy.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import org.tues.tudy.R

// Fredoka – only one file, used for headings/titles
val PrimaryFont = FontFamily(
    Font(R.font.fredoka)  // single file
)

// Lato – only one file, used for body text
val SecondaryFont = FontFamily(
    Font(R.font.lato)     // single file
)
